# E2.2 — User Handlers

## Описание

Создание handlers для базовых команд бота: /start, /profile, /help.

---

## Tasks

### T2.2.1 — Start handler

**Описание:** Создать handler для команды `/start`.

**Файл:** `backend/src/bot/handlers/start.py`

**Логика:**
```python
from aiogram import Router
from aiogram.filters import CommandStart
from aiogram.types import Message

router = Router()

@router.message(CommandStart())
async def cmd_start(message: Message, user: User) -> None:
    """
    1. Приветствие пользователя по имени
    2. Показ краткой информации о боте
    3. Показ главного меню (reply keyboard)
    """
```

**Сообщение:**
```
Привет, {first_name}! 👋

Я помогу тебе управлять подпиской и токенами.

Используй меню ниже или команды:
/profile — твой профиль
/tariffs — доступные тарифы
/help — справка
```

**DoD:**
- [ ] Handler отвечает на `/start`
- [ ] Показывает персонализированное приветствие
- [ ] Показывает reply keyboard с меню
- [ ] Работает для новых и существующих пользователей

---

### T2.2.2 — Profile handler

**Описание:** Создать handler для команды `/profile` (или `/me`).

**Файл:** `backend/src/bot/handlers/profile.py`

**Логика:**
```python
@router.message(Command("profile", "me"))
async def cmd_profile(message: Message, user: User) -> None:
    """
    Показать профиль пользователя:
    - Telegram ID
    - Username (если есть)
    - Баланс токенов
    - Статус подписки (активна до / не активна)
    """
```

**Сообщение:**
```
📊 Твой профиль

🆔 ID: {user.id}
👤 Username: @{user.username or "не указан"}

💰 Баланс: {user.token_balance} токенов
📅 Подписка: {subscription_status}

Пополнить баланс: /tariffs
```

**Форматирование подписки:**
- Активна: `активна до {date}`
- Не активна: `не активна`
- Истекает сегодня: `истекает сегодня!`

**DoD:**
- [ ] Handler отвечает на `/profile` и `/me`
- [ ] Показывает все поля профиля
- [ ] Корректно форматирует статус подписки
- [ ] Обрабатывает отсутствие username

---

### T2.2.3 — Help handler

**Описание:** Создать handler для команды `/help`.

**Файл:** `backend/src/bot/handlers/help.py`

**Логика:**
```python
@router.message(Command("help"))
async def cmd_help(message: Message) -> None:
    """Показать справку по командам"""
```

**Сообщение:**
```
📖 Справка

Доступные команды:

/start — начать работу
/profile — твой профиль
/tariffs — доступные тарифы
/balance — текущий баланс
/history — история транзакций
/help — эта справка

По вопросам: @support_username
```

**DoD:**
- [ ] Handler отвечает на `/help`
- [ ] Показывает список всех команд
- [ ] Контакт поддержки настраивается через config
